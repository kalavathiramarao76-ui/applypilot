{
  "name": "applypilot",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=@applypilot/web",
    "dev:ext": "turbo run dev --filter=@applypilot/extension",
    "dev:mobile": "turbo run dev --filter=@applypilot/mobile",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=@applypilot/web",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "db:generate": "pnpm --filter @applypilot/shared run db:generate",
    "db:push": "pnpm --filter @applypilot/shared run db:push",
    "db:studio": "pnpm --filter @applypilot/shared run db:studio"
  }
}
